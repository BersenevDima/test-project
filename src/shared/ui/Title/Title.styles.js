import styled from 'styled-components';
import { Colors } from '../../styles';

export const Wrapper = styled.h2`
  font-size: 20px;
  font-family: 'Inter';
  font-weight: 600;
  color: ${Colors.Dune};
`;
